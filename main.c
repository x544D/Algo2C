#include "saad_Lib/mylib.h" //matmse7ch hadi

int main()
{
int  a,b;
Ecrire("Donnez un Nombre ; ");
//Lire(a);
if(a <= 10)
{
    while(b >= 0) {
        Ecrire(b);
    }

}
else if(a <= 50)
{
    Ecrire("Wow");
}
else
{
    Ecrire("Rip");
}

return 0; // matmse7ch hadi
}